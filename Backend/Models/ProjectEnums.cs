

namespace Backend.Models
{
    public static class ProjectEnums
    {
        public enum CourseStatus { Public, Private }
        public enum UserType { Instructor, Student, Admin }
        public enum NotificationType { Reply, Marketing }
    }
}